// import {Router} from 'express';
// import loginController from '../controller/loginController';

// const rotaLogin = Router();

// rotaLogin.post("/login", loginController.login)
// rotaLogin.post("/", loginController.createClient)
// rotaLogin.put("/:id", loginController.updateClient)

// export default rotaLogin;
